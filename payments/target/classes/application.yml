spring:
  datasource:
    url: jdbc:mysql://localhost/payment?createDatabaseIfNotExist=true&useSSL=false
    username: root
  #    password:
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8050
